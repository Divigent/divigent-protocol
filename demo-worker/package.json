{
  "name": "divigent-demo-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/worker.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "ethers": "^6.13.5"
  },
  "devDependencies": {
    "wrangler": "^4.13.2"
  }
}
